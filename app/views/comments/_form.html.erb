<%= simple_form_for(@comment, html: { class: 'form-horizontal' }) do |form| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>s
  <% end %>
    <%#= form.input  :user_id, collection: User.all, label_method: :full_name %><br>
    <%= form.input  :Name %><br>
     <%= form.input  :Comment %>

  <div class="form-actions">
    <%= form.button :submit %>
  </div>
<% end %>
